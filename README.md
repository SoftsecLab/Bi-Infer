We propose Bi-Infer, a novel black-box LLM source tracing method based on bidirectional contextual inference, which constructs probability fluctuation matrices via selective masking and local entropy normalization and adopts a Conformer network with masked majority voting to identify the source LLM of generated text, achieving superior performance over existing SOTA approaches across multiple benchmarks.
